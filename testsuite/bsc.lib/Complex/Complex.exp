
test_c_veri_bsv CmplxTest

# This compare was XFAILed under verilator for a signed part-select
# $display misprint (Verilator 5.048 dropped the field mask on a low
# part-select wrapped in $signed()).  "verilator: preserve signed
# part-select values" changed the emitted Verilog so the trigger no
# longer appears in generated code, and the test passes under
# verilator; the XFAIL key is retired so an XPASS does not trip the
# CI verdict.
test_c_veri_bsv CmplxSatAdd


